<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Consultoria Esportiva - Kokoro</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }

        :root {
            --bg1: #0a1427;
            --bg2: #162540;
            --accent: #4a90e2;
            --glass-bg: rgba(74, 144, 226, 0.1);
            --glass-border: rgba(74, 144, 226, 0.3);
            --text: #ffffff;
            --text-muted: rgba(255, 255, 255, 0.6);
            --success: #4ecca3;
            --warning: #ff9f43;
            --danger: #ee5a6f;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, var(--bg1), var(--bg2));
            color: var(--text);
            min-height: 100vh;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 40px;
            background: var(--glass-bg);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid var(--glass-border);
        }

        .header h1 { 
            font-size: 28px; 
            font-weight: 700; 
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .header-actions {
            display: flex;
            gap: 15px;
            align-items: center;
        }

        .icon-btn {
            background: var(--glass-bg);
            border: 1px solid var(--glass-border);
            color: var(--text);
            width: 45px;
            height: 45px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            cursor: pointer;
            transition: all 0.3s;
        }

        .icon-btn:hover {
            background: var(--accent);
            transform: translateY(-2px);
        }

        .dashboard-layout {
            display: grid;
            grid-template-columns: 260px 1fr;
            height: calc(100vh - 85px);
        }

        .sidebar {
            background: var(--glass-bg);
            backdrop-filter: blur(10px);
            border-right: 1px solid var(--glass-border);
            padding: 30px 20px;
            overflow-y: auto;
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 15px 20px;
            margin-bottom: 10px;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s;
            color: var(--text);
            text-decoration: none;
        }

        .nav-item:hover {
            background: var(--glass-bg);
            transform: translateX(5px);
        }

        .nav-item.active {
            background: var(--accent);
            color: #000;
            font-weight: 600;
        }

        .nav-item .icon {
            font-size: 22px;
            width: 30px;
        }

        .content {
            padding: 30px 40px;
            overflow-y: auto;
        }

        .view {
            display: none;
        }

        .view.active {
            display: block;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: var(--glass-bg);
            backdrop-filter: blur(10px);
            border: 1px solid var(--glass-border);
            border-radius: 16px;
            padding: 24px;
            transition: all 0.3s;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        .stat-card .icon {
            font-size: 36px;
            margin-bottom: 15px;
        }

        .stat-card .value {
            font-size: 32px;
            font-weight: 700;
            color: var(--accent);
            margin-bottom: 5px;
        }

        .stat-card .label {
            color: var(--text-muted);
            font-size: 14px;
        }

        .table-container {
            background: var(--glass-bg);
            backdrop-filter: blur(10px);
            border: 1px solid var(--glass-border);
            border-radius: 16px;
            padding: 24px;
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th {
            text-align: left;
            padding: 12px;
            color: var(--accent);
            border-bottom: 2px solid var(--glass-border);
            font-weight: 600;
        }

        td {
            padding: 15px 12px;
            border-bottom: 1px solid var(--glass-border);
        }

        tr:hover {
            background: rgba(255,255,255,0.02);
        }

        .form-card {
            background: var(--glass-bg);
            backdrop-filter: blur(10px);
            border: 1px solid var(--glass-border);
            border-radius: 16px;
            padding: 30px;
            max-width: 800px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            color: var(--text);
        }

        .form-control {
            width: 100%;
            padding: 12px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid var(--glass-border);
            border-radius: 10px;
            color: var(--text);
            font-family: inherit;
            font-size: 14px;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--accent);
        }

        textarea.form-control {
            resize: vertical;
            min-height: 100px;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }

        .btn {
            padding: 12px 24px;
            background: var(--accent);
            border: none;
            border-radius: 10px;
            color: #000;
            font-weight: 600;
            font-size: 16px;
            cursor: pointer;
            transition: all 0.3s;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(74, 144, 226, 0.3);
        }

        .btn-full {
            width: 100%;
            padding: 14px;
        }

        .badge {
            padding: 6px 12px;
            border-radius: 8px;
            font-size: 12px;
            font-weight: 600;
        }

        .badge-success {
            background: rgba(78, 204, 163, 0.2);
            color: var(--success);
        }

        .badge-warning {
            background: rgba(255, 159, 67, 0.2);
            color: var(--warning);
        }

        .badge-danger {
            background: rgba(238, 90, 111, 0.2);
            color: var(--danger);
        }

        .alert {
            padding: 12px 16px;
            border-radius: 10px;
            margin-bottom: 20px;
        }

        .alert-success {
            background: rgba(78, 204, 163, 0.2);
            border: 1px solid rgba(78, 204, 163, 0.5);
            color: var(--success);
        }

        .alert-warning {
            background: rgba(255, 159, 67, 0.2);
            border: 1px solid rgba(255, 159, 67, 0.5);
            color: var(--warning);
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: var(--text-muted);
        }

        .empty-state .icon {
            font-size: 64px;
            margin-bottom: 20px;
            opacity: 0.5;
        }

        @media (max-width: 768px) {
            .dashboard-layout {
                grid-template-columns: 1fr;
            }
            .sidebar {
                display: none;
            }
            .form-row {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
                <polyline points="3.5 12 7 12 9 7 11 17 13 12 16.5 12"></polyline>
            </svg>
            Consultoria Saúde Esportiva
        </h1>
        <div class="header-actions">
            <button class="icon-btn" title="Notificações">🔔</button>
            <button class="icon-btn" onclick="window.location.href='/admin_Dashboard.html'" title="Voltar ao Admin">🏠</button>
        </div>
    </div>

    <div class="dashboard-layout">
        <aside class="sidebar">
            <nav>
                <a href="javascript:void(0)" class="nav-item active" onclick="showView('visao-geral')">
                    <span class="icon">📊</span>
                    <span>Visão Geral</span>
                </a>
                <a href="javascript:void(0)" class="nav-item" onclick="showView('atendimentos')">
                    <span class="icon">🩺</span>
                    <span>Atendimentos</span>
                </a>
                <a href="javascript:void(0)" class="nav-item" onclick="showView('agendamentos')">
                    <span class="icon">📅</span>
                    <span>Agenda</span>
                </a>
                <a href="javascript:void(0)" class="nav-item" onclick="showView('pacientes')">
                    <span class="icon">👥</span>
                    <span>Pacientes</span>
                </a>
                <a href="javascript:void(0)" class="nav-item" onclick="showView('avaliacoes')">
                    <span class="icon">📋</span>
                    <span>Avaliações</span>
                </a>
                <a href="javascript:void(0)" class="nav-item" onclick="showView('relatorios')">
                    <span class="icon">📈</span>
                    <span>Relatórios</span>
                </a>
                <a href="javascript:void(0)" class="nav-item" onclick="showView('configuracoes')">
                    <span class="icon">⚙️</span>
                    <span>Perfil</span>
                </a>
            </nav>
        </aside>

        <main class="content">
            <div id="alertContainer"></div>

            <!-- VISÃO GERAL -->
            <div id="visao-geral" class="view active">
                <h2 style="margin-bottom: 20px;">📊 Visão Geral</h2>

                <div class="stats-grid">
                    <div class="stat-card">
                        <div class="icon">👥</div>
                        <div class="value">0</div>
                        <div class="label">Pacientes Ativos</div>
                    </div>

                    <div class="stat-card">
                        <div class="icon">📅</div>
                        <div class="value">0</div>
                        <div class="label">Consultas Hoje</div>
                    </div>

                    <div class="stat-card">
                        <div class="icon">✅</div>
                        <div class="value">0</div>
                        <div class="label">Consultas Realizadas</div>
                    </div>

                    <div class="stat-card">
                        <div class="icon">⭐</div>
                        <div class="value">5.0</div>
                        <div class="label">Avaliação Média</div>
                    </div>
                </div>

                <div class="table-container">
                    <h3 style="margin-bottom: 15px;">📅 Próximas Consultas</h3>
                    <div class="empty-state">
                        <div class="icon">📭</div>
                        <p>Nenhuma consulta agendada para hoje</p>
                    </div>
                </div>
            </div>

            <!-- ATENDIMENTOS -->
            <div id="atendimentos" class="view">
                <h2 style="margin-bottom: 20px;">🩺 Atendimentos</h2>
                <div class="table-container">
                    <div class="empty-state">
                        <div class="icon">🩺</div>
                        <p>Nenhum atendimento no momento</p>
                    </div>
                </div>
            </div>

            <!-- AGENDAMENTOS -->
            <div id="agendamentos" class="view">
                <h2 style="margin-bottom: 20px;">📅 Agenda de Consultas</h2>
                <div class="table-container">
                    <div class="empty-state">
                        <div class="icon">📅</div>
                        <p>Nenhuma consulta agendada</p>
                    </div>
                </div>
            </div>

            <!-- PACIENTES -->
            <div id="pacientes" class="view">
                <h2 style="margin-bottom: 20px;">👥 Meus Pacientes</h2>
                <div class="table-container">
                    <div class="empty-state">
                        <div class="icon">👥</div>
                        <p>Nenhum paciente cadastrado</p>
                    </div>
                </div>
            </div>

            <!-- AVALIAÇÕES -->
            <div id="avaliacoes" class="view">
                <h2 style="margin-bottom: 20px;">📋 Avaliações Físicas</h2>
                <div class="table-container">
                    <div class="empty-state">
                        <div class="icon">📋</div>
                        <p>Nenhuma avaliação registrada</p>
                    </div>
                </div>
            </div>

            <!-- RELATÓRIOS -->
            <div id="relatorios" class="view">
                <h2 style="margin-bottom: 20px;">📈 Relatórios</h2>
                <div class="table-container">
                    <div class="empty-state">
                        <div class="icon">📈</div>
                        <p>Nenhum relatório disponível</p>
                    </div>
                </div>
            </div>

            <!-- CONFIGURAÇÕES (PERFIL) -->
            <div id="configuracoes" class="view">
                <h2 style="margin-bottom: 20px;">⚙️ Configurações do Perfil Profissional</h2>

                <div class="form-card">
                    <form id="perfilForm">
                        <div class="form-row">
                            <div class="form-group">
                                <label class="form-label">Nome Completo *</label>
                                <input type="text" class="form-control" id="nome" required>
                            </div>

                            <div class="form-group">
                                <label class="form-label">Email *</label>
                                <input type="email" class="form-control" id="email" required>
                            </div>
                        </div>

                        <div class="form-row">
                            <div class="form-group">
                                <label class="form-label">Telefone</label>
                                <input type="tel" class="form-control" id="telefone" placeholder="(00) 00000-0000">
                            </div>

                            <div class="form-group">
                                <label class="form-label">Área de Atuação *</label>
                                <select class="form-control" id="area_atuacao" required>
                                    <option value="">Selecione...</option>
                                    <option value="Educação Física">🏃 Educação Física</option>
                                    <option value="Medicina Esportiva">🩺 Medicina Esportiva</option>
                                    <option value="Fisioterapia">🦴 Fisioterapia</option>
                                    <option value="Nutrição">🥗 Nutrição</option>
                                    <option value="Psicologia do Esporte">🧠 Psicologia do Esporte</option>
                                </select>
                            </div>
                        </div>

                        <div class="form-row">
                            <div class="form-group">
                                <label class="form-label">Número de Registro *</label>
                                <input type="text" class="form-control" id="numero_registro" placeholder="CREF, CRM, CRP, CRN..." required>
                            </div>

                            <div class="form-group">
                                <label class="form-label">Valor da Consulta (R$)</label>
                                <input type="number" class="form-control" id="valor_consulta" placeholder="150.00" step="0.01">
                            </div>
                        </div>

                        <div class="form-group">
                            <label class="form-label">Especializações/Formações</label>
                            <textarea class="form-control" id="especializacoes" placeholder="Ex: Especialista em Nutrição Esportiva, MBA em Gestão..."></textarea>
                        </div>

                        <div class="form-group">
                            <label class="form-label">Resumo Profissional *</label>
                            <textarea class="form-control" id="resumo" placeholder="Fale brevemente sobre sua experiência e áreas de atuação..." required></textarea>
                        </div>

                        <button type="submit" class="btn btn-full">💾 Salvar Perfil</button>
                    </form>
                </div>
            </div>
        </main>
    </div>

    <script>
        function showView(viewId) {
            document.querySelectorAll('.nav-item').forEach(item => {
                item.classList.remove('active');
            });

            event.target.closest('.nav-item').classList.add('active');

            document.querySelectorAll('.view').forEach(view => {
                view.classList.remove('active');
            });

            document.getElementById(viewId).classList.add('active');
        }

        document.getElementById('perfilForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const dados = {
                nome: document.getElementById('nome').value,
                email: document.getElementById('email').value,
                telefone: document.getElementById('telefone').value,
                area_atuacao: document.getElementById('area_atuacao').value,
                numero_registro: document.getElementById('numero_registro').value,
                especializacoes: document.getElementById('especializacoes').value,
                resumo: document.getElementById('resumo').value,
                valor_consulta: document.getElementById('valor_consulta').value
            };

            console.log('Salvando perfil:', dados);
            mostrarAlerta('✅ Perfil salvo com sucesso!', 'success');
        });

        function mostrarAlerta(mensagem, tipo) {
            const container = document.getElementById('alertContainer');
            container.innerHTML = `<div class="alert alert-${tipo}">${mensagem}</div>`;
            
            setTimeout(() => {
                container.innerHTML = '';
            }, 5000);
        }
    </script>
</body>
</html>